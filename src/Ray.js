import React from 'react'

class Ray extends React.Component {
    constructor (props) {
        super(props)
    }

    render () {
        // get source
        // project
        return (
            <div id="ray">
            </div>
        )
    }
}

export default Ray;